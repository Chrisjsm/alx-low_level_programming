#include "main.h"
/**
 * _strlen - returns the len of a str
 * @s: string
 * Return: len
 */
int swap_int(char *s)
{
        int longi = 0;
	while (*s != '\0')
	{
		longi++;
		s++;
	}
	return (longi);

}

